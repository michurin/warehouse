package user_test

import (
	"testing"
)

func Test(t *testing.T) {
	/*
		u := user.New()
		u.Update("xxx", 10, "Nik", "#000")
		b, err := json.Marshal(user.JSON(u))
		if err != nil {
			panic(err) // TODO assert
		}
		t.Log(string(b)) // TODO assert

		u.Lock()
		u.Update("xxx2", 10, "Nik2", "#111") // won't work. locked
		b, err = json.Marshal(user.JSON(u))
		if err != nil {
			panic(err) // TODO assert
		}
		t.Log(string(b)) // TODO assert

		u.Unlock()
		u.Update("xxx", 11, "Nik2", "#111")
		b, err = json.Marshal(user.JSON(u))
		if err != nil {
			panic(err) // TODO assert
		}
		t.Log(string(b)) // TODO assert
	*/
}
