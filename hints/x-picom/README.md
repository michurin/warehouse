Solving problems with Google chrome context menu and
similar things.

It helps:

```
blur-background-exclude = [
  "window_type = 'menu'";
  "window_type = 'dropdown_menu'";
  "window_type = 'popup_menu'";
  "window_type = 'tooltip'";
];
```
